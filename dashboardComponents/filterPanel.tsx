import { Box } from "@primer/react";
import { NextPage } from "next";

interface Props {}

const filterPanel: NextPage<Props> = ({}) => {
  return <Box>filterPanel</Box>;
};

export default filterPanel;
